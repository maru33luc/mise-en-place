import { Component, inject } from '@angular/core';
import { ActivatedRoute, RouterLink } from '@angular/router';
import { RecipeService } from '@core/services/recipe.service';
import type { Recipe } from '@core/models/recipe.model';
@Component({selector:'app-recipe-detail',standalone:true,imports:[RouterLink],template:`<main class="detail">@if(recipe; as item){<a routerLink="/recipes" class="back">← Collection</a><p class="eyebrow">{{item.season ?? 'all'}} season · {{item.difficulty}}</p><h1>{{item.title}}</h1><p class="description">{{item.description}}</p><div class="ingredients"><h2>Ingredients <small>{{scaledServings}} portions</small></h2>@for(ingredient of item.ingredients; track ingredient.name){<div><span>{{ingredient.name}}</span><b>{{scale(ingredient.amount)}} {{ingredient.unit}}</b></div>}</div>} @else {<p>Loading recipe...</p>}</main>`,styles:[`:host{display:block}.detail{max-width:900px;margin:0 auto;padding:9rem 1.5rem 5rem}.back{color:#a18f72;text-decoration:none;font:600 .68rem Inter;text-transform:uppercase;letter-spacing:.14em}.eyebrow{color:#c9a96e;text-transform:uppercase;font:600 .68rem Inter;letter-spacing:.2em;margin-top:4rem}h1{color:#eee0c8;font:500 clamp(3rem,8vw,7rem)/.95 'Playfair Display';margin:1rem 0}.description{color:#a69b8c;font:1.1rem/1.8 Inter;max-width:600px}.ingredients{margin-top:5rem;border-top:1px solid #2b261f}.ingredients h2{color:#eee0c8;font:500 2rem 'Playfair Display';padding:1.5rem 0}.ingredients small{float:right;color:#c9a96e;font:.62rem Inter;text-transform:uppercase;letter-spacing:.1em}.ingredients div{display:flex;justify-content:space-between;padding:1rem 0;border-top:1px solid #1d1b18;color:#9f9586;font:.8rem Inter}.ingredients b{color:#d4b87a;font-weight:500}`]}) export class RecipeDetailComponent { private readonly service=inject(RecipeService); private readonly route=inject(ActivatedRoute); recipe:Recipe|null=null; scaledServings=0; constructor(){const id=Number(this.route.snapshot.paramMap.get('id'));this.service.getById(id).subscribe(r=>{if(r.data){this.recipe=r.data;this.scaledServings=r.data.servings??2;}});} scale(amount:number){return amount;} }
